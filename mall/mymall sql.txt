create database mymall;

member table

no int auto_increment primary key,
    -> userid char(20) unique,
    -> name char(20),
    -> pwd char(10),
    -> phone char(20),
    -> email varchar(100),
    -> writeday date,
    -> state int default 0);
	
create table product(
no int auto_increment primary key,
pcode char(12),
pimg varchar(100),
cimg varchar(100),
title varchar(100),
price int,
halin int,
color char(12),
size char(12),
su int,
bprice int,
btime int,
juk int,
pansu int,
writeday date);

create table dae
no int auto_increment primary key,
name char(20),
code char(2)

create table jung
no int auto_increment primary key.
name char(20),
code char(2),
daecode(2));

